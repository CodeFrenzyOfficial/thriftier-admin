name: Admin Site Staging Deployment

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      NODE_VERSION: "20.x"

    steps:
      - name: 🚀 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: 📥 Install dependencies
        run: npm ci

      - name: 🔧 Create .env.production file
        run: |
          cat > .env.production << 'EOF'
          VITE_API_URL=${{ secrets.STAGING_API_URL }}
          VITE_APP_NAME=${{ secrets.VITE_APP_NAME }}
          VITE_APP_ENV=staging
          EOF

      - name: 🏗️ Build application
        run: npm run build
        env:
          NODE_ENV: production

      - name: 📋 Create deployment package
        run: |
          cd dist
          tar -czf ../frontend-build.tar.gz .
          cd ..

      - name: 🔐 Setup SSH key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.STAGING_SSH_KEY }}

      - name: 📤 Deploy to staging server
        env:
          STAGING_HOST: ${{ secrets.STAGING_HOST }}
          STAGING_USER: ${{ secrets.STAGING_USER }}
          DEPLOY_PATH: /var/www/thrifter-admin-staging
        run: |
          # Add server to known hosts
          ssh-keyscan -H $STAGING_HOST >> ~/.ssh/known_hosts

          # Upload deployment package
          scp frontend-build.tar.gz $STAGING_USER@$STAGING_HOST:$DEPLOY_PATH/

          # Execute deployment script on server
          ssh $STAGING_USER@$STAGING_HOST "
            cd $DEPLOY_PATH

            # Backup existing dist folder
            if [ -d 'dist' ]; then
              rm -rf dist.backup
              mv dist dist.backup
            fi

            # Create fresh dist directory
            mkdir -p dist

            # Extract new version directly to dist
            tar -xzf frontend-build.tar.gz -C dist/

            # Cleanup tar file
            rm frontend-build.tar.gz

            # Reload Nginx to serve new files
            sudo systemctl reload nginx || echo 'Nginx reload skipped (run manually if needed)'

            echo '✅ Thrifter Admin deployed successfully to $DEPLOY_PATH/dist'
          "

      - name: ✅ Deployment successful
        run: |
          echo "🎉 Thrifter Admin deployed to staging successfully!"
          echo "📂 Server path: /var/www/thrifter-admin-staging/dist"
          echo "🔗 Access your admin panel at: https://admin.gothriftier.com"
          echo "📅 Deployed at: $(date)"

      - name: 🚨 Notify on failure
        if: failure()
        run: |
          echo "❌ Admin site deployment failed!"
          echo "📋 Please check the logs above for details."
          echo "💡 Common issues: SSH key, server path permissions, nginx config"
